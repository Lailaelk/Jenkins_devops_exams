apiVersion: v1
kind: Service

metadata:
  name: cast-service
  labels:
    app: cast-service

spec:
  selector:
    app: cast-service

  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP

  type: ClusterIP
